name: Build web amd64 image

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: Image tag to publish
        required: true
        default: latest
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  build-web:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set image metadata
        id: meta
        shell: bash
        run: |
          owner_lower=$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            tags="ghcr.io/${owner_lower}/langfuse-zh-web:${{ github.event.inputs.image_tag }}"
          else
            tags="ghcr.io/${owner_lower}/langfuse-zh-web:main-${GITHUB_SHA::7}"
            tags="${tags},ghcr.io/${owner_lower}/langfuse-zh-web:latest"
          fi
          echo "tags=${tags}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push web image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./web/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          build-args: |
            BUILDPLATFORM=linux/amd64
            NEXT_PUBLIC_BUILD_ID=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
